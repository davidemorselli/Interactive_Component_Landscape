# What a model is asked, and what is done with what it answers — `rag` hands
# over a list of tweets, and everything from there to a graded analysis is
# here. `prompts` is every prompt the app sends and calls no model; `pipeline`
# is the write/grade/refine process and its four levels. Neither knows Flask:
# `webapp` drives them.
